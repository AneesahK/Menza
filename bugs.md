When adding a note, and removing it, old conversation keep the same notes (should I keep the notes or should it follow whatever is in the assitant notes section?)
- it is specific to the conversation, i should inject it into each query given and not just initially at the start
    - maybe if it changes then I should re-inject it with the users new query instead of setting it for each chat